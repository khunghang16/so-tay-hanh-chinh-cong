# Video lớn (~1.7GB, mỗi file tới 302MB) — GitHub chặn file >100MB.
# Tải lại bằng: bash download-videos.sh
files/editor/files/extfile/*.mp4

# macOS / editor cruft
.DS_Store
*.log
*.bak

# Bản backup ảnh/video gốc (không commit, chỉ giữ local)
_backup/
